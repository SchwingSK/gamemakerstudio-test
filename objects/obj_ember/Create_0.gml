speed = 0;
direction = 0;

// All of these variables are randomized in User Event 0.
// base_direction is the direction that this ember is moving in
base_direction = 0;
// osc_direction is the direction value added to the base direction for
// oscillating its direction, giving a waving effect
osc_direction = 0;
// osc_amount is how many degrees are added for oscillation at max
osc_amount = 0;
// osc_speed is how fast it oscillates
osc_speed = 0;
// fade_rate is how fast this ember fades out
fade_rate = 0;